/*import React, { Component, useState, useEffect } from "react";
import Select from 'react-select'
import SEPractices from "../dummydata/SEPractices"
import axios, * as others from "axios";
  
/*const [Evidence, setEvidence] = useState("");

const fetchData = async () => {
    const res = await axios.get(`http://localhost:5000/BookDetailsEvidence`);
    setEvidence(res.data);
  };

const optionItems = SEPractice.map((SEPractice) => (
  <option key={SEPractice.practice}>{SEPractice.practice}</option>
));*/
/*const optionItems = [
  { value: 'chocolate', label: 'Chocolate' },
  { value: 'strawberry', label: 'Strawberry' },
  { value: 'vanilla', label: 'Vanilla' }*/

/*const levelOfEvidenceDropdown = ({Evidencedrop}) => {
  return (
    <Select
    isMulti
    name="colors"
    options={Evidencedrop}
    className="EvidenceDropdown"
    classNamePrefix="Evidence"
  />
  );
};
/*const levelOfEvidenceDropdown= ({}) => {
    return (
        <div>
             <select>
                {optionItems}
             </select>
         </div>
    )
  }*/
  //export default levelOfEvidenceDropdown;